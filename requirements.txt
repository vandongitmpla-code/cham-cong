# Web framework (giữ version cố định để tránh xung đột)
Flask==2.3.3
Werkzeug==2.3.7
Jinja2==3.1.2
itsdangerous==2.1.2

# Database ORM (giữ bản ổn định tránh lỗi TypingOnly)
SQLAlchemy==1.4.49
Flask-SQLAlchemy==3.0.5

# Migration
alembic==1.12.0
Flask-Migrate==4.0.4

# Template engine (fix version tránh lỗi html_escape)
Mako==1.2.4

# Utilities
click==8.1.7

# Data processing (để tự chọn version mới nhất)
pandas
openpyxl
xlrd

# PostgreSQL driver
psycopg2-binary

# Optional (highlight, debug)
#pygments
#python-dotenv
